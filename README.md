# Assignment
To run the api or project 
Step 1 : open project in vscode and write command in terminal npm install for the install node modules.
Step 2: after npm install write command npm start after writting this command project will work on local machine
Step 3 : for testing API use postman and check the APIs.
